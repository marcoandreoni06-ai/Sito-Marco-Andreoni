/** Pixel-font section label with a leading gradient tick. */
export default function Eyebrow({ children, className = '' }) {
  return <span className={`eyebrow text-muted ${className}`}>{children}</span>
}
